[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# 환경 변수는 Netlify 대시보드에서 설정하세요
# VITE_SUPABASE_PROJECT_ID
# VITE_SUPABASE_ANON_KEY